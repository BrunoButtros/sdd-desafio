# Log de Decisões e Mudanças de Spec

> Uma entrada **toda vez** que a spec mudar. Este arquivo é a prova de que a spec
> foi tratada como artefato vivo e não como cerimônia de abertura.
>
> Spec que não muda em dois dias é spec que ninguém consultou. Mudança não é
> demérito — mudança não registrada é.

Ordem cronológica inversa: a mais recente primeiro.

---

## D-003 — Envelope do Dia 2 e política v4 · `2026-08-05`

**Gatilho:** recebimento do envelope oficial do Dia 2, liberado às 10h — mudança de requisito do RH, política de reembolso v4, com vigência imediata e retroativa à competência atual. Cinco arquivos oficiais acompanham o envelope e foram recebidos e colocados em `exemplos/envelope/` sem alteração; serão versionados no commit documental correspondente: `00-ENVELOPE-LACRADO.md` (comunicado do RH), `politica-v4.json` (tabela de limites por centro de custo), `cambio.json` (taxas de câmbio por data), `despesas-envelope.json` (Rafael Nkemelu, `CC-COMERCIAL`, exercitando moeda estrangeira) e `despesas-envelope-cc-desconhecido.json` (Dani Okonkwo, `CC-SUPORTE-N2`, exercitando centro de custo fora da tabela). Uma sessão de análise anterior a esta (modo leitura, sem alteração de arquivo) já havia mapeado o comunicado item a item, calculado os quatro cenários financeiros abaixo manualmente e identificado as ambiguidades candidatas contra a lista de análise obrigatória do próprio processo. Esta entrada formaliza as decisões tomadas sobre essas ambiguidades e atualiza `spec.md` para a versão 1.2. Uma revisão independente subsequente, na mesma data, encontrou oito problemas de precisão de contrato — nenhum deles muda um total financeiro já registrado — e as correções estão incorporadas nesta mesma entrada, na seção "Correção após revisão independente" ao final. Uma segunda revisão independente, também na mesma data, encontrou pontos adicionais de precisão de contrato e uma lacuna de vocabulário de motivos — incorporados na seção "Ajustes finais da segunda revisão independente", ao final desta entrada.

**O que mudou na spec:**

1. **Política deixa de ser um valor único (RN-019, nova).** A spec 1.1 não previa política externa nem variação por centro de custo — RN-007 e RN-011/012/013 tratavam os limites como constantes universais. A v1.2 introduz resolução por `colaborador.centro_custo`: tabela `padrao` como fallback integral; centro de custo cadastrado usa somente as categorias que declara, sem fallback por categoria; limite explicitamente `0,00` em uma tabela de centro cadastrado recusa a categoria (`CATEGORIA_NAO_REEMBOLSAVEL_CENTRO_CUSTO`), nunca reembolso parcial de valor zero; limite `0,00` em `padrao` invalida estruturalmente o arquivo de política; categoria adicional (`representacao`) só é reembolsável nos centros que a declaram.
2. **Câmbio e conversão monetária (RN-020, nova).** A spec 1.1 declarava explicitamente, em `§3`, que o sistema "não converte nem identifica moedas" e que "todos os valores são interpretados como reais brasileiros" — essa frase foi removida e substituída (ver "comportamento anterior invalidado" abaixo). `despesa.moeda` é novo campo opcional — o **sétimo** dos oito campos de 4.2, entre `valor` (6º) e `tem_nota_fiscal` (8º); a ausência da **chave** resolve para `BRL` sem produzir motivo, mas `"moeda": null` (chave presente, valor `null`) é `CAMPO_AUSENTE`, e presença malformada de outro jeito recusa o item como qualquer campo estrutural (correção pós-revisão, ver ao final). A resolução de taxa usa cotação exata ou a mais recente anterior disponível, nunca futura, sem interpolação; moeda sem cotação utilizável recusa o item (`MOEDA_SEM_COTACAO`). Conversão e arredondamento têm ordem única: multiplicar, depois arredondar uma vez (RN-004 atualizada).
3. **RN-009 (nota fiscal) atualizada.** O gatilho de R$100 agora compara explicitamente o valor já convertido para BRL, não o valor original na moeda da despesa — caso contrário, USD 40,00 (abaixo do gatilho na moeda original) escaparia da obrigatoriedade de nota apesar de valer R$220,00 convertido.
4. **RN-010 (duplicidade) atualizada.** A chave econômica passa a ser `data`, `categoria` normalizada, `moeda`, `valor` normalizado **em BRL** (já convertido, RN-020), `fornecedor` original e `descricao` original — `moeda` entra como componente **adicional**, ao lado do valor já convertido, não em lugar dele. Dois itens em moedas diferentes nunca são duplicatas entre si, mesmo que o valor convertido coincida exatamente: é `moeda` que impede a colisão, não o fato de o valor estar ou não convertido. Usar o valor em BRL mantém a chave de duplicidade alinhada ao mesmo número que todas as regras financeiras posteriores usam.
5. **RN-017 e o contrato de saída (4.3) atualizados.** Todo registro de resultado ganha três campos novos, entre `valor_informado` e `valor_normalizado`: `moeda`, `taxa_cambio_aplicada` e `data_cotacao_utilizada` — necessários para que a conversão seja auditável sem reabrir `cambio.json` manualmente.
6. **RN-021 (nova) — vigência do arquivo de política.** `politica.vigencia` é obrigatória e validada estruturalmente (mesma severidade de RN-001, mas sobre o arquivo de política, não o de despesas); uma vez válida, é metadado informativo — não filtra itens por data, porque nenhuma política anterior à v4 foi fornecida para justificar seleção histórica.
7. **RN-022 (nova) — processabilidade dos arquivos externos.** Política ou câmbio ausente, ilegível, sintaticamente inválido ou estruturalmente inválido é erro global: nenhuma apuração é produzida, mesmo com um arquivo de despesas perfeitamente válido.
8. **RN-002, RN-004, RN-005, RN-007 atualizadas** para refletir o sétimo campo (`despesa.moeda`), a ordem conversão-então-arredondamento, e o conjunto de categorias reembolsáveis deixando de ser uma constante de três valores.
9. **Dezenove ambiguidades novas registradas (`AMB-019` a `AMB-037`)**, cobrindo: centro de custo desconhecido/ausente (AMB-019), categoria ausente em centro cadastrado (AMB-020), `representacao` exclusiva de quem a declara (AMB-021), limite igual a zero (AMB-022), moeda como indício de viagem (AMB-023), data sem cotação exata (AMB-024), moeda sem cotação alguma (AMB-025), ordem de conversão/arredondamento (AMB-026), base de comparação do gatilho de nota fiscal (AMB-027), chave de duplicidade entre moedas (AMB-028), campos de auditoria da conversão (AMB-029), validação de vigência (AMB-030), erro global versus recusa individual nos arquivos externos (AMB-031), regressão da baseline histórica (AMB-032), escopo do item C (AMB-033), as três acrescentadas na correção pós-revisão — forma de fornecimento de política/câmbio à CLI (AMB-034), contrato estrutural dos arquivos externos (AMB-035) e semântica de `periodicidade` (AMB-036) —, e a acrescentada na segunda correção pós-revisão: motivo de teto individual para categoria externa de periodicidade `"diaria"` diferente de hospedagem (AMB-037).
10. **Vinte e seis critérios de aceite novos (`CA-024` a `CA-049`)**: `CA-024` a `CA-040` da primeira leitura do envelope, incluindo os quatro cenários financeiros completos do `§12` (nova seção); `CA-041` a `CA-048` da correção pós-revisão, cobrindo o contrato de execução, os contratos estruturais dos arquivos externos, a periodicidade diária de `representacao` e `despesa.moeda: null`; `CA-049` da segunda correção pós-revisão, cobrindo o motivo `TETO_INDIVIDUAL_APLICADO`.

**Comportamento anterior invalidado:**

- A frase de `§3` "Não converte nem identifica moedas. Todos os valores são interpretados como reais brasileiros (BRL)" deixa de ser verdadeira e foi removida — substituída por uma declaração sobre o que continua fora de escopo (inferência de viagem a partir da moeda).
- RN-007 na forma "apenas `alimentacao`, `transporte_urbano` e `hospedagem` são reembolsáveis", como conjunto fechado e universal, deixa de valer sem qualificação — o conjunto reembolsável agora depende da política aplicável ao centro de custo (RN-019).
- RN-011, RN-012 e RN-013 deixam de tratar R$60/R$80/R$250 como constantes absolutas da regra — passam a ser os valores da política **padrão**, com centros de custo cadastrados podendo declarar valores próprios ou tornar a categoria não reembolsável.
- A garantia de que `colaborador.centro_custo` "nunca é usado para cruzar dados" (§3, sobre agregação entre arquivos) permanece verdadeira, mas a leitura mais ampla de que o campo não tem "uso em regra alguma" deixa de valer: `centro_custo` passa a determinar a política aplicável dentro do próprio arquivo (RN-019). A tolerância estrutural do bloco `colaborador` (RN-001) não muda — só o efeito financeiro de um `centro_custo` presente e reconhecido.
- `§11`, "o que fica em aberto", removeu a linha "Apenas BRL é suportado" — deixou de ser uma limitação de produto e passou a ser requisito atendido.

**Ambiguidades identificadas:** as dezenove listadas no item 9 acima, com decisão, justificativa e regra afetada registradas em `spec.md §6` (`AMB-019` a `AMB-037`).

**Decisões dos quatro grupos** (política por centro de custo; câmbio; conversão e regras financeiras; saída e vigência):

- **Política por centro de custo:** `padrao` como fallback integral; centro cadastrado usa só suas categorias declaradas, sem fallback por categoria; limite explicitamente `0,00` em uma tabela de centro cadastrado recusa a categoria com `CATEGORIA_NAO_REEMBOLSAVEL_CENTRO_CUSTO`; limite `0,00` em `padrao` invalida estruturalmente o arquivo de política (código de saída `2`, correção da segunda revisão); comparação de `centro_custo` é textual e exata.
- **Câmbio:** cotação exata ou a mais recente anterior; nunca futura; sem interpolação; sem cotação utilizável recusa o item.
- **Conversão e regras financeiras:** um único arredondamento após a multiplicação; nota fiscal e duplicidade operam sobre o valor convertido (duplicidade com moeda como componente adicional da chave, não substituindo o valor).
- **Saída e vigência:** três campos de auditoria novos na saída; vigência obrigatória e validada, mas informativa quanto a itens individuais.

**RN e CA antigos afetados:** RN-002, RN-004, RN-005, RN-007, RN-009, RN-010, RN-011, RN-012, RN-013, RN-014, RN-015, RN-017 (textos atualizados, sem troca de número; RN-007 recebeu correção adicional na revisão, e RN-011 a RN-015 receberam correção adicional na segunda revisão, generalizando o mecanismo de teto por `periodicidade` — ver ao final). Nenhum RN ou CA de `1.1` foi removido ou renumerado — `RN-001` a `RN-018` e `CA-001` a `CA-023` permanecem intactos onde não listados acima.

**RN e CA novos:** `RN-019` a `RN-022` (nenhuma RN além de `RN-022` foi criada — as correções pós-revisão só atualizaram RN-019 e RN-022, sem numeração nova); `CA-024` a `CA-049`.

**DT-007 invalidada.** `plan.md` ainda declara, em DT-007, "estrutura única e imutável... sem mecanismo genérico de regras... sem arquivo externo de configuração" para `PoliticaReembolso` — essa decisão técnica está tecnicamente incorreta a partir desta versão da spec e será revista quando `plan.md` for atualizado, em etapa posterior. Esta entrada apenas formaliza a invalidação do lado da spec; nenhuma linha de `plan.md` foi tocada nesta rodada.

**Regressão financeira sob a v4:** processar `exemplos/despesas-exemplo.json` com a política **padrão** (equivalente aos valores hardcoded até a v1.1) continua totalizando R$585,43 — baseline histórica preservada e testável (`CA-037`). O mesmo arquivo, processado com a política **oficial v4** e o centro de custo real do colaborador (`CC-ENG-PLATAFORMA`, que tem entrada própria na tabela), totaliza **R$351,43** — `d-001` passa a integral (R$72,50), `d-002` a parcial (R$2,50), `d-010` a recusado por `CATEGORIA_NAO_REEMBOLSAVEL_CENTRO_CUSTO` (R$0,00, era R$250,00 parcial), `d-014` a integral (R$61,00). A diferença de R$234,00 é consequência direta e correta da política v4 aplicada ao centro de custo real do colaborador do arquivo — não é um bug, mas precisa estar documentada para que ninguém confunda a mudança de total com uma regressão de código quando `plan.md`/`tasks.md`/os testes forem atualizados. Os dois cenários coexistem como critérios de aceite distintos (`CA-037` e `CA-038`), preservando a baseline histórica em vez de escolher entre "manter" e "atualizar" o fixture original.

**Item C fora de escopo.** A fila de aprovação manual (`AGUARDANDO_APROVACAO` para itens acima de R$500) não foi especificada nesta rodada — é explicitamente opcional no próprio comunicado do RH, e nenhum item dos dois arquivos de exemplo do envelope ultrapassa R$500 sob a interpretação adotada, então nada nos dados obriga essa decisão agora (`AMB-033`).

**Custo documental e técnico previsto:** esta entrada e a atualização de `spec.md` (cabeçalho, `§0` nova, `§3`, `§4.1`/`4.1.1` nova e depois reescrita nas duas correções, `§4.2`, `§4.3`, `§4.5`, `§4.7`, `§5` com doze RN atualizadas e quatro novas (as doze atualizadas: `RN-002`, `RN-004`, `RN-005`, `RN-007`, `RN-009`, `RN-010`, `RN-011`, `RN-012`, `RN-013`, `RN-014`, `RN-015` e `RN-017`) — várias delas corrigidas de novo nas revisões —, `§6` com dezenove AMB novas, `§7`, `§8.1` a `8.5`, `§9`, `§10` com vinte e seis CA novas, `§11`, `§12` nova) — nenhum outro arquivo tocado nesta rodada. `plan.md` precisará de: nova DT substituindo DT-007, extensão de DT-003 (CLI, agora com o contrato exato já fixado em `spec.md` `§4.1.1`) e DT-005 (parsing) para os arquivos externos, e um novo passo no pipeline (§2). `tasks.md` precisará de tasks novas a partir de `T-022` para: leitor de política, leitor de câmbio, campo `moeda` no contrato do item (incluindo a distinção ausência-de-chave versus `null`), conversão cambial no pipeline, resolução de política por centro de custo (incluindo a semântica de `periodicidade`), motivo `CATEGORIA_NAO_REEMBOLSAVEL_CENTRO_CUSTO`, motivo `MOEDA_SEM_COTACAO`, três campos novos de saída, validação de `vigencia`, os quatro argumentos de CLI com seus códigos de saída, validação estrutural dos dois arquivos externos, e os quatro cenários de regressão do `§12` como fixtures de teste.

**Tasks afetadas:** nenhuma ainda — `plan.md` e `tasks.md` serão atualizados em etapas posteriores a esta, que é estritamente normativa (`spec.md` e este arquivo).

### Correção após revisão independente (mesma data, `2026-08-05`)

Uma revisão independente da primeira leitura do envelope — antes de qualquer código ser escrito — encontrou oito problemas de precisão de contrato. Nenhum deles altera um total financeiro já registrado nesta entrada (R$585,43 / R$351,43 / R$1.143,26 / R$373,76 permanecem exatamente como acima); todos são correções de exatidão normativa.

1. **`despesa.moeda` é o sétimo campo do contrato, não o oitavo.** O contrato tem oito campos; sete são obrigatórios, mas não são "os sete primeiros" — `despesa.moeda`, o único opcional, ocupa a sétima posição, entre `despesa.valor` (6º) e `despesa.tem_nota_fiscal` (8º, também obrigatório). Texto corrigido em `spec.md` `§4.2` e nesta entrada (item 2 acima).
2. **`despesa.moeda: null` não é mais tolerado como a ausência da chave.** Só a **ausência da própria chave** `moeda` no objeto resolve para `BRL` sem motivo. `"moeda": null` — chave presente, valor `null` — é `CAMPO_AUSENTE`, `campo` igual a `despesa.moeda`, como qualquer campo obrigatório. Tipo não textual continua `CAMPO_TIPO_INVALIDO`; formato fora de `[A-Z]{3}` continua `CAMPO_FORMATO_INVALIDO`. Novo critério `CA-048`.
3. **Contrato exato da CLI incorporado à spec (`AMB-034`, nova).** `spec.md` `§4.1.1` passa a declarar, normativamente: `calcular --input <e> --output <s> --politica <p> --cambio <c>`, quatro argumentos obrigatórios em qualquer ordem, cada um uma única vez; argumento ausente/repetido/desconhecido, ou política/câmbio inválidos, retornam código `2` (sem código `4`/`5` novo); envelope de despesas inválido continua código `3`; sucesso continua `0`; saída preexistente preservada em qualquer falha. A frase anterior, que reservava nomes de argumento e códigos de saída exclusivamente ao `plan.md`, foi removida — o contrato de execução é comportamento observável do produto e é normativo da spec. Novos critérios `CA-041` a `CA-044`.
4. **Contrato estrutural fechado dos dois arquivos externos (`AMB-035`, nova).** A versão anterior desta correção descrevia `cambio.json` como um mapa direto de data para moeda — **incorreto**: a raiz real tem `moeda_base`, `fonte`, `observacao` e as cotações aninhadas sob `taxas` (`taxas.AAAA-MM-DD.USD` etc.). `spec.md` `§4.1.1` agora documenta o contrato completo de ambos os arquivos — que campos são obrigatórios, seus tipos e restrições (`limite` ≥ 0, `periodicidade` exatamente `"dia"` ou `"diaria"`, taxas estritamente positivas) — tornando "estruturalmente inválido" (RN-022) verificável sem leitura de código. `taxas: {}` é explicitamente válido (recusa despesas estrangeiras item a item, não invalida o arquivo). Novos critérios `CA-045`, `CA-046`.
5. **Semântica de `periodicidade` formalizada (`AMB-036`, nova; RN-019 atualizada).** `"dia"` é teto compartilhado entre itens da mesma categoria e data (mecanismo de RN-011/RN-012/RN-015); `"diaria"` é teto individual por lançamento (mecanismo de RN-013). `representacao` em `CC-COMERCIAL` usa `periodicidade: "dia"`, logo dois itens de `representacao` na mesma data compartilham o saldo de R$300,00 — não são tetos individuais de R$300,00 cada. `periodicidade` fora desse vocabulário fechado invalida o **arquivo de política**, não o item. Novo critério `CA-047`.
6. **RN-007 e RN-019 corrigidas: nunca há duas tabelas de política simultaneamente aplicáveis.** O texto anterior desta entrada, ao dizer que a categoria era verificada contra "padrão e, quando houver, específica do centro de custo", sugeria as duas tabelas ativas ao mesmo tempo — o que a própria RN-019 já contradizia (centro cadastrado usa **somente** a sua tabela). Corrigido para: exatamente uma tabela é aplicável a cada item — `padrao` quando o centro de custo é desconhecido/ausente/nulo/inválido, ou exclusivamente a tabela do centro cadastrado, quando houver. Consequência: quando o centro é cadastrado, **toda** ausência de categoria nessa tabela — inclusive uma categoria que não existe em lugar nenhum da política, como `coworking` — é `CATEGORIA_NAO_REEMBOLSAVEL_CENTRO_CUSTO`, não `CATEGORIA_FORA_POLITICA`; esta última passa a significar exclusivamente "ausente de `padrao`, quando `padrao` é a tabela aplicável". Isso muda o **motivo** do item `e-009` (`coworking`, Rafael/`CC-COMERCIAL`) no cenário do `§12.3`, de `CATEGORIA_FORA_POLITICA` para `CATEGORIA_NAO_REEMBOLSAVEL_CENTRO_CUSTO` — o `valor_reembolsavel` (`0,00`) e o total de Rafael (`R$1.143,26`) **não mudam**. `AMB-021` (`representacao`) também foi corrigida para não tratar essa categoria como caso especial: segue a mesma régua de qualquer categoria ausente de uma tabela cadastrada.
7. **Exemplo de conversão cambial corrigido.** O exemplo de "moeda estrangeira convertida" em `spec.md` `§4.3` pareava `taxa_cambio_aplicada: 5.93` com `data_cotacao_utilizada: "2026-07-17"` — errado: `5.93` é a cotação de EUR em `2026-07-14`; a cotação de `2026-07-17` é `5.96`. Corrigido para `5.96`/`2026-07-17`. O cálculo de `e-004` no `§12.3` (`30,00 × 5,96 = 178,80`) já usava o valor correto desde a primeira leitura — o erro estava isolado no fragmento ilustrativo de `§4.3`, não nos totais.
8. **Bloco `colaborador`: precisão sobre "regra financeira".** A frase "o bloco não participa de regra financeira alguma" (`§4.1`) deixou de ser universalmente verdadeira desde que `centro_custo` passou a selecionar política (item 1 desta entrada) — substituída por uma descrição precisa: `colaborador.id`/`colaborador.nome` continuam sem uso em regra alguma; `colaborador.centro_custo`, quando texto e reconhecido, seleciona a política (RN-019); ausente/nulo/inválido/desconhecido usa `padrao`; nenhum defeito do bloco invalida o arquivo.

**Ambiguidades e critérios desta correção:** `AMB-034`, `AMB-035`, `AMB-036`; `CA-041` a `CA-048`. Contagem final desta correção: `AMB-001` a `AMB-036` (36), `RN-001` a `RN-022` (22, nenhuma nova além de `RN-022`), `CA-001` a `CA-048` (48) — superada pela segunda correção abaixo.

### Ajustes finais da segunda revisão independente (mesma data, `2026-08-05`)

Uma segunda revisão independente, posterior à correção acima, encontrou pontos adicionais de precisão de contrato e uma lacuna no vocabulário de motivos de teto. Nenhum deles altera um total financeiro já registrado nesta entrada (R$585,43 / R$351,43 / R$1.143,26 / R$373,76 permanecem exatamente como acima); todos são correções de exatidão normativa ou fechamento de vocabulário.

1. **Critérios históricos passaram a declarar a política externa usada.** `CA-001`, `CA-002`, `CA-004` a `CA-009`, `CA-013`, `CA-016` e `CA-018`, os aceites de `RN-011` a `RN-015` e os casos de borda de `spec.md` `§7` que usam `60`, `80`, `250` ou `100` agora declaram explicitamente que esses valores vêm da política externa histórica/padrão usada no cenário — não são constantes internas do sistema. `RN-009` foi atualizada com a mesma precisão sobre `nota_fiscal_obrigatoria_acima_de`.
2. **`CA-026` foi alinhado a `AMB-021` e `RN-019`.** A redação anterior afirmava que `representacao` fora de `CC-COMERCIAL` sempre recebe `CATEGORIA_FORA_POLITICA` — contradizia a régua de tabela única de `RN-019`/`AMB-021`, que distingue `padrao` (sem `representacao` → `CATEGORIA_FORA_POLITICA`) de um centro cadastrado que não a declara (→ `CATEGORIA_NAO_REEMBOLSAVEL_CENTRO_CUSTO`). Corrigido.
3. **Limite zero em `padrao` passou a invalidar o arquivo de política.** O contrato de `4.1.1` (`AMB-035`) permitia `limite: 0` em qualquer tabela. Fechado: em `padrao`, todo `limite` deve ser estritamente maior que zero — `padrao` é o fallback universal, e um teto zero ali retiraria uma categoria de todo centro de custo desconhecido sem que o comunicado expresse essa decisão dessa forma; `limite: 0` em `padrao` torna o arquivo de política estruturalmente inválido (RN-022), falha global, código de saída `2`. Em `centros_custo`, `limite: 0` continua sendo decisão válida do próprio centro (`CATEGORIA_NAO_REEMBOLSAVEL_CENTRO_CUSTO`). Nenhum motivo de item novo foi criado para o caso de `padrao` — a falha é do arquivo inteiro, antes de qualquer item ser avaliado.
4. **`AMB-037` (nova) resolveu categoria externa com `periodicidade: "diaria"` fora de `hospedagem`.** `AMB-036` já permitia qualquer categoria com `periodicidade: "diaria"`, mas o único motivo de teto individual existente, `TETO_HOSPEDAGEM_APLICADO`, é semanticamente específico de hospedagem.
5. **Periodicidade passou a governar o mecanismo, não mais o nome histórico da categoria.** `RN-011` a `RN-015` foram reescritas para que o mecanismo — teto compartilhado por data, ou teto individual por lançamento — decorra de `periodicidade` na tabela de política efetivamente aplicável; o comportamento histórico de `alimentacao`/`transporte_urbano` (compartilhado) e `hospedagem` (individual) continua valendo porque é o que a política padrão declara, não porque o nome da categoria é especial.
6. **Foi criado o motivo `TETO_INDIVIDUAL_APLICADO`** (contrato completo em `AMB-037`: `codigo: TETO_INDIVIDUAL_APLICADO`, `regra: RN-019`, `campo: null`), para qualquer categoria diferente de `hospedagem` com `periodicidade: "diaria"` cujo valor normalizado exceda o limite individual.
7. **Foi criado `CA-049`**, cenário de `estacionamento` (`limite` R$50,00, `periodicidade: "diaria"`, despesa elegível de R$80,00, `PARCIALMENTE_REEMBOLSADO`, `valor_reembolsavel` R$50,00, motivo `TETO_INDIVIDUAL_APLICADO`).
8. **Os quatro totais financeiros já registrados nesta entrada permaneceram inalterados** — R$585,43 (baseline histórica), R$351,43 (exemplo antigo sob v4), R$1.143,26 (Rafael) e R$373,76 (Dani) — nenhuma das correções acima altera cálculo de item ou de total já publicado.

**Ambiguidades e critérios desta segunda correção:** `AMB-037`; `CA-049`. Contagem final: `AMB-001` a `AMB-037` (37), `RN-001` a `RN-022` (22, nenhuma nova), `CA-001` a `CA-049` (49).

Uma conferência final de consistência alinhou RN-020 e a matriz de dependências ao tratamento de `MOEDA_SEM_COTACAO`, tornou a redação de RN-013 condicional à periodicidade configurada, generalizou as linhas de teto diário e individual e qualificou os dois comportamentos distintos de limite zero. Nenhuma decisão, ambiguidade, regra, critério ou total foi alterado.

---

## D-002 — Ajustes de contrato após auditoria independente · `2026-07-30`

**Gatilho:** auditoria independente da spec 1.0 aprovada, realizada em sessão separada da elaboração original, com recomputação manual dos 14 itens de `exemplos/despesas-exemplo.json` a partir das regras (não da tabela 4.7) e leitura cruzada de `spec.md` com este arquivo. A auditoria confirmou o total de R$ 585,43 e as 14 decisões, sem nenhuma divergência de cálculo, mas encontrou três problemas de gravidade média, todos de precisão de contrato — nenhum deles altera resultado do arquivo de exemplo.

**O que mudou na spec:**

1. **Classificação dos erros estruturais (4.2, RN-002, 4.5, §7).** A spec distinguia `CAMPO_TIPO_INVALIDO` de `CAMPO_FORMATO_INVALIDO` sem enunciar o critério geral — só havia dois exemplos pontuais. Passa a existir regra fechada: ausência ou nulo → `CAMPO_AUSENTE`; tipo JSON errado → `CAMPO_TIPO_INVALIDO`; tipo certo com conteúdo que viola restrição → `CAMPO_FORMATO_INVALIDO`. Booleano nunca é aceito como número, nem o inverso. Sete exemplos normativos foram adicionados em 4.2, e os casos de borda (§7) ganharam quatro linhas novas (categoria numérica, data com formato certo e calendário inexistente, nota fiscal como texto, valor booleano).
2. **`valor_informado` (4.3).** O tipo era descrito como "número, conteúdo recebido ou nulo" — união pouco clara que não dizia o que acontece quando `despesa.valor` chega com tipo inválido. Passa a ser "qualquer valor JSON ou nulo": preserva exatamente o que foi recebido em `despesa.valor`, mesmo com tipo errado (`"72,50"` continua `"72,50"`, `true` continua `true`); é nulo quando a chave está ausente, nula, ou quando o elemento de `despesas` não é objeto. `valor_normalizado` continua nulo sempre que `despesa.valor` não for número válido — isso não mudou, só ficou explícito que é independente de `valor_informado`.
3. **Matriz de dependências (8.2).** Faltava a linha de RN-003 (unicidade de `despesa.id`), apesar de a dependência já estar implícita em 8.4, item 6. Adicionada.

Ajustes de clareza que acompanharam a correção, sem mudar comportamento: a coluna "Vazio permitido" de 4.2 virou "Restrição adicional", com o texto da coluna "Tipo" simplificado para o tipo JSON puro; a exclusão de nota fiscal por valor não positivo (8.4, item 10) ganhou uma frase justificando por que é mantida mesmo sendo derivável da própria condição de RN-009 — fecha a porta para uma leitura por valor absoluto (ex.: −R$ 500,00 não exigir nota). Acrescentado `CA-023`, cobrindo simultaneamente a classificação estrutural e a preservação de `valor_informado` num único item com cinco campos malformados.

**Por quê:** os três problemas médios eram pontos onde duas implementações igualmente fiéis ao texto anterior da spec podiam divergir no código de saída — exatamente o tipo de fronteira que casos ocultos de avaliação costumam explorar. Fechar agora custa uma revisão textual; deixar aberto arriscava pontos no critério de produto por um motivo que não é erro de cálculo.

**O que isso invalidou:** nenhum cálculo dos 14 itens de `exemplos/despesas-exemplo.json` mudou — decisão, valor reembolsável e motivos de cada um permanecem idênticos aos da linha de base, e o total continua R$ 585,43. Nenhuma AMB, subdecisão ou RN foi criada, removida ou renumerada. A contagem de critérios de aceite passa de 22 (`CA-001` a `CA-022`, como registrado em D-001) para 23 (`CA-001` a `CA-023`) nesta versão 1.1.

**Tasks afetadas:** nenhuma — `tasks.md` ainda não foi elaborado; esta auditoria antecede o Dia 1 tarde.

**Custo:** alteração documental em `spec.md` (seções 4.2, 4.3, 5/RN-002, 4.5, 7, 8.2, 8.4, 9 e 10) e nesta entrada de `DECISIONS.md`. Nenhum arquivo de código tocado.

---

## D-001 — Linha de base da spec 1.0 · `2026-07-30`

**Gatilho:** análise da política de reembolso v3 cruzada com `exemplos/despesas-exemplo.json`, item a item, antes de qualquer linha de spec ser escrita. A política é ambígua em pontos que alteram o valor pago, e não havia interlocutor no RH para esclarecer.

**O que mudou na spec:** primeira versão. Não havia spec anterior — este é o marco zero, não uma alteração.

Foram identificadas **18 ambiguidades** (`AMB-001` a `AMB-018`) e 15 subdecisões qualificadas, distribuídas pelos quatro tipos:

| Tipo | Ambiguidades |
|---|---|
| Unidade de aplicação | AMB-001, AMB-005, AMB-007, AMB-011, AMB-017 |
| Fronteira | AMB-003, AMB-010, AMB-014, AMB-015 |
| Dado ausente | AMB-006, AMB-008, AMB-009, AMB-013 |
| Outra | AMB-002, AMB-004, AMB-012, AMB-016, AMB-018 |

As 15 subdecisões são `AMB-006/JANELA`, `AMB-008/FORMATO`, `AMB-008/LOCAL`, `AMB-009/FONTE`, `AMB-011/POPULACAO`, `AMB-012/CLASSIFICACAO`, `AMB-013/PISO`, `AMB-013/GATILHO`, `AMB-013/DUPLICIDADE`, `AMB-015/ESCOPO`, `AMB-015/CONTRATO`, `AMB-015/ENVELOPE`, `AMB-016/NF`, `AMB-016/EXCLUSOES` e `AMB-018/ESCOPO`.

Delas nasceram 18 regras de negócio (`RN-001` a `RN-018`) e 22 critérios de aceite (`CA-001` a `CA-022`).

**Por quê:** a política v3 enuncia condições sem enunciar consequências, usa unidades que a entrada não representa e não declara precedência entre as nove regras. Sem decidir explicitamente cada ponto, dois processamentos corretos da mesma entrada produziriam valores diferentes — que é exatamente o problema que o motor existe para resolver.

Três princípios transversais governaram as decisões e valem registro porque se repetem em várias ambiguidades:

1. **O conteúdo semântico de texto livre não é interpretado.** `descricao` e `fornecedor` não são lidos para inferir viagem, quantidade de diárias, estorno, categoria ou qualquer outro tratamento financeiro; são usados somente em comparação literal de igualdade na chave de duplicidade. Aplicado em AMB-006 (viagem não é inferida de "aeroporto"/"hotel"), AMB-008 (diárias não são extraídas de "2 diarias"), AMB-013 (estorno não é reconhecido pela palavra "estorno") e AMB-015/ESCOPO (`descricao` e `fornecedor` não são normalizados). A fronteira é essa: comparar duas descrições exatamente iguais para detectar duplicidade é permitido; interpretar o significado de uma palavra não é.
2. **Campo estruturado ausente torna a regra inerte, não inventada.** Aplicado em AMB-006 e AMB-007, que deixam a regra 6 da política sem efeito em vez de criar um proxy.
3. **Nenhum lançamento desaparece do resultado.** Aplicado em AMB-018: todo item da entrada tem exatamente um registro de saída, inclusive os recusados.

**O que isso invalidou:** nada. Não havia artefato anterior.

**Tasks afetadas:** nenhuma ainda — `tasks.md` será escrito a partir desta linha de base.

**Custo:** dois arquivos preenchidos a partir dos templates copiados, `spec.md` e este log. A análise que os precedeu percorreu os 14 itens do arquivo de exemplo e resolveu as ambiguidades em dez rodadas de decisão.

### Revisões durante a elaboração

As decisões abaixo mudaram **durante** a análise, antes de a spec existir como arquivo. Não houve versão anterior publicada e não há entrada retroativa fingindo que houve — mas o registro do que mudou e por quê é a evidência de que as decisões foram testadas contra os dados em vez de escolhidas de primeira.

**R-1 · Escopo de AMB-001 restringido a duas categorias.**
Originalmente a agregação por categoria e data valia para as três categorias. Ao decidir AMB-008, ficou evidente que hospedagem tem limite "por diária" e não "por dia" — unidades diferentes. A agregação passou a valer somente para alimentação e transporte urbano; hospedagem é avaliada por lançamento. *Efeito:* forçou ajuste de redação em AMB-002, AMB-017 e AMB-018, que haviam sido escritas presumindo que toda categoria agregava por dia.

**R-2 · AMB-002 e AMB-017 se contradiziam na letra.**
AMB-002 afirmava que "a despesa não será recusada integralmente"; AMB-017 determinava valor zero para itens posteriores ao esgotamento do teto. Aplicadas a `d-001` e `d-002`, produziam zero em `d-002` — violando AMB-002. *Correção:* AMB-002 passou a garantir o **agregado diário**, e AMB-017 passou a declarar que zero por esgotamento **não é recusa**. Foi o que criou os estados distintos `NAO_REEMBOLSADO_TETO_ESGOTADO` e `RECUSADO` no vocabulário de saída.

**R-3 · AMB-005 redefinida sobre o valor normalizado.**
A decisão original falava em "valor bruto individual", significando o valor como veio no arquivo. Ao decidir AMB-014, a normalização monetária passou a ocorrer antes de todas as validações, e "bruto" mudou de sentido: passou a significar *após a normalização de contrato, antes de qualquer corte por teto*. *Efeito:* deslocou a fronteira efetiva da nota fiscal de `100.000` para `100.005` no valor informado.

**R-4 · AMB-018 ampliada duas vezes.**
Escrita inicialmente com uma justificativa por item. AMB-016 exigiu que todos os motivos aplicáveis fossem reportados, e a decisão sobre unicidade de `despesa.id` exigiu um identificador de saída que sobrevivesse a ID ausente, inválido ou repetido. *Resultado:* a saída passou a ter uma decisão final e **uma ou mais** justificativas, mais o campo `indice_entrada` de base 1.

**R-5 · Ordem canônica ampliada com a unicidade de ID.**
A ordem inicial tinha os passos estruturais seguidos diretamente das regras individuais de negócio. A verificação de `despesa.id` repetido — descoberta ao revisar o contrato de entrada, não ao ler a política — exigiu um passo próprio entre as duas etapas, porque é violação de rastreabilidade e não de identidade econômica. *Efeito:* a ordem de processamento e a ordem de apresentação dos motivos deixaram de coincidir, e a spec passou a declará-las separadamente na seção 8.

**R-6 · Correção de identificadores.**
Durante a análise, três identificadores provisórios foram atribuídos incorretamente porque o número da pergunta foi confundido com o identificador da ambiguidade — as três decisões consumiram identificadores reservados a outras ambiguidades. Corrigidas, tornaram-se as subdecisões qualificadas `AMB-006/JANELA`, `AMB-008/FORMATO` e `AMB-008/LOCAL`, devolvendo os identificadores que hoje pertencem a AMB-010, AMB-014 e AMB-016. A partir daí, toda subdecisão passou a usar sufixo qualificado em vez de consumir identificador novo. Registro aqui porque a numeração é o eixo da rastreabilidade e um erro nela se propaga por tasks, commits e testes.

**R-7 · Omissão encontrada na conferência final.**
A decisão sobre metadados do colaborador cobria `colaborador.nome` e `colaborador.centro_custo`, mas o arquivo de exemplo tem um terceiro campo, `colaborador.id`, que ficara sem destino. Foi incluído como metadado opcional sem exigência de unicidade, e a spec passou a usar nomes qualificados — `colaborador.id` contra `despesa.id` — porque a regra `ID_DUPLICADO` alcança apenas o segundo.

**R-8 · Correções da revisão da linha de base.**
A leitura integral da spec 1.0, antes de qualquer commit, encontrou quatro defeitos de conteúdo que foram corrigidos ainda dentro desta linha de base. **(a)** O princípio "texto livre não determina resultado financeiro" era absoluto demais e contradizia AMB-011, porque `descricao` e `fornecedor` integram a chave exata de duplicidade e podem, por essa via, alterar o resultado; foi reescrito para restringir a proibição ao conteúdo semântico. **(b)** RN-016 e CA-010 afirmavam que duas entradas idênticas exceto pela descrição produzem os mesmos valores — o que é falso quando há duplicidade em jogo; passaram a testar a ausência de inferência de viagem num cenário de item único. **(c)** O aceite de RN-002 listava os motivos estruturais em ordem inversa à do contrato, com `valor` antes de `data`. **(d)** O contrato exigia que cada elemento de `despesas` fosse objeto, mas não dizia o que acontece quando não é; nasceu daí o motivo `ITEM_TIPO_INVALIDO` e o critério CA-022. Na mesma revisão, `motivos` deixou de ser lista de códigos e passou a ser lista de objetos com `codigo`, `regra` e `campo`, para satisfazer a exigência de AMB-018 de vincular cada justificativa à regra que a produziu, e o comportamento do bloco `colaborador` malformado foi fechado.

Três ajustes finais fecharam a linha de base, ainda sem publicação: **(e)** a recusa por erro estrutural passou a enumerar as quatro condições — campo ausente, nulo, de tipo inválido e de **formato inválido** —, que antes apareciam incompletas em 4.2, RN-002, AMB-015/CONTRATO e na lista de exclusões; **(f)** o registro de saída de uma posição que não é objeto passou a ser declarado campo a campo, incluindo `valor_informado` nulo, que não estava explícito; **(g)** `motivo.campo` ganhou lista fechada de sete valores canônicos na forma `despesa.<campo>`, eliminando a oscilação entre `valor` e `despesa.valor` nos exemplos, na ordem canônica, na matriz de dependências, em RN-002, CA-021 e CA-022. Com isso a spec passou de "em revisão" para **aprovada**, na mesma versão 1.0.
